<Project>
  <PropertyGroup>
    <ManagePackageVersionsCentrally>true</ManagePackageVersionsCentrally>
  </PropertyGroup>
  <ItemGroup>
    <PackageVersion Include="PhoenixmlDb.Core" Version="1.7.0" />
    <!-- Used by the xquery CLI tool to wire fn:transform via XsltTransformProvider
         (auto-registered by [ModuleInitializer] in PhoenixmlDb.Xslt 1.2.8+). -->
    <!-- Deliberately one release behind the rest of the family, and the only exception to
         the uniform-version rule introduced in 1.6.5.

         PhoenixmlDb.XQuery.Cli needs PhoenixmlDb.Xslt (for fn:transform), while
         PhoenixmlDb.Xslt needs PhoenixmlDb.XQuery. At the LIBRARY layer there is no cycle —
         PhoenixmlDb.XQuery depends only on Core — but this repo publishes the library and
         the CLI together at one version, so pinning Xslt 1.6.5 here would require Xslt 1.6.5
         to exist before the thing it depends on has been published.

         The `xquery` tool therefore embeds the PREVIOUS release's XSLT engine: 1.6.5 in the
         1.6.6 train. That is the standing shape of this pin, not a temporary lag — it moves
         forward one release each train, always trailing by one.

         THAT FIX IS NOW IN PLACE. The library and the CLI publish on separate tags:

             v<version>      library only — depends on Core, which runs its own cadence
             cli-v<version>  CLI only — tagged after the Xslt of this train is published,
                             so this pin can and must equal the train version

         So this is no longer a standing lag. It is tolerance for one window only: between
         Xslt <train> being published and this repo bumping to it. Bump it as soon as the Xslt
         of the train is live, then tag cli-v<version>.

         Both markers below are deliberate and not redundant. check-pins.sh tolerates the
         window; check-release-train.sh demands equality at the moment the CLI is packed, which
         is the only moment it matters. Tolerance between trains, equality at the tag — trailing
         by one used to be permanent, and it hid a skipped train when this sat at 1.6.11 through
         both 1.6.12 and 1.6.13.
         check-pins: trails-by-1
         check-pins: train-locked -->
    <PackageVersion Include="PhoenixmlDb.Xslt" Version="1.6.15" />
    <PackageVersion Include="Antlr4.Runtime.Standard" Version="4.13.1" />
    <PackageVersion Include="Antlr4BuildTasks" Version="12.14.0" />
    <PackageVersion Include="Lucene.Net" Version="4.8.0-beta00016" />
    <PackageVersion Include="Lucene.Net.Analysis.Common" Version="4.8.0-beta00016" />
    <PackageVersion Include="FluentAssertions" Version="8.2.0" />
    <PackageVersion Include="Microsoft.NET.Test.Sdk" Version="17.14.0" />
    <PackageVersion Include="xunit" Version="2.9.3" />
    <PackageVersion Include="xunit.runner.visualstudio" Version="3.1.0" />
    <PackageVersion Include="StreamJsonRpc" Version="2.25.29" />
  </ItemGroup>
</Project>
